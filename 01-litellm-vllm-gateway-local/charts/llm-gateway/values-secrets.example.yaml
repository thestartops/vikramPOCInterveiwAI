# Copy this file to values-secrets.yaml (that filename is gitignored) and
# fill in real values. NEVER commit the real values-secrets.yaml.
#
#   cp values-secrets.example.yaml values-secrets.yaml
#   $EDITOR values-secrets.yaml
#   helm install llm-gateway . \
#     -f values-local-kind.yaml \
#     -f values-secrets.yaml

secrets:
  vllmApiKey: "changeme-vllm-key"
  azure:
    apiKey: ""
    apiBase: "https://<your-resource>.openai.azure.com"
    apiVersion: "2024-02-15-preview"
